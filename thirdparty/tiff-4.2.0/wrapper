#!/bin/sh
#
# This just ensures that libtiff can be built without ccache
# and not change the rest of the build process.

CC="gcc" CXX="g++" ./configure --with-pic --disable-jbig --disable-webp

make

